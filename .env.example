DATABASE_URL="postgresql://postgres:postgres@localhost:5432/vishwakarma_kutumb?schema=public"
JWT_SECRET="change-me-to-a-long-random-string"
JWT_EXPIRES_IN="7d"
PORT=3000

# OTP provider — this app ships a console-log mock (OtpProviderService).
# Swap it for a real SMS gateway (MSG91, Twilio, etc.) by implementing the
# same `send(phone, code)` interface.
OTP_MOCK_CODE=345021
